{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && python manage.py collectstatic --noinput"
  },
  "deploy": {
    "startCommand": "python manage.py migrate --noinput; python -c \"import django,os;os.environ.setdefault('DJANGO_SETTINGS_MODULE','espiadimonis.settings');django.setup();from django.contrib.auth.models import User;User.objects.filter(email='admin@espiadimonis.cat').exists() or User.objects.create_superuser('admin','admin@espiadimonis.cat','Espiadimonis2026!')\"; gunicorn espiadimonis.wsgi --bind 0.0.0.0:$PORT --workers 2 --log-file -",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
